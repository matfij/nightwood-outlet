apiVersion: skaffold/v4beta2
kind: Config
manifests:
  rawYaml:
    - .infra\k8s\*.yaml
build:
  local:
    push: false
  artifacts:
    - image: matfij/nw-auth
      context: apps/auth
      docker:
        dockerfile: Dockerfile
      sync:
        manual:
          - dest: .
            src: 'src/**/*.ts'
